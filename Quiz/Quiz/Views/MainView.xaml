<UserControl x:Class="Quiz.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quiz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="QUIZ" FontSize="150" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center"/>
        <Button Style="{DynamicResource ButtonStyle}" Background="#2E86DE" Foreground="#ffffff" BorderThickness="0"
                FontSize="24" FontWeight="Light" FontFamily="Inter"
                Grid.Column="0" Content="Create" Height="80" Width="90" Command="{Binding NavigateToCreateCommand}"/>
        <Button Style="{DynamicResource ButtonStyle}" Background="#2E86DE" Foreground="#ffffff" BorderThickness="0"
                FontSize="24" FontWeight="Light" FontFamily="Inter" 
                Grid.Column="1" Content="Search" Height="80" Width="90" Command="{Binding NavigateToSearchCommand}"/>
    </Grid>
</UserControl>
